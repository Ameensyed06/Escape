# Optional — the app already ships with the ESCAPE Supabase project's URL
# and anon key baked in (see src/lib/supabase.ts), same as the mobile app.
# Only set these if you want to point the website at a different project.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
